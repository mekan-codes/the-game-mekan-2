MODEL TO USE: GPT-5.5 Extensive

Continue from the current SDL2 C game:

Crane Operator: Safe Cargo Delivery

I have provided a new polished v11 asset pack in:

assets/

This v11 pack is much higher quality than the old v10 pack. Integrate it carefully.

Do NOT rewrite the game from scratch.
Do NOT break physics.
Do NOT break the 6 levels.
Do NOT break mouse clicking.
Do NOT break fullscreen/full-view.
Do NOT modify Assignment9 or Assignment10.
Do NOT replace working gameplay with a prototype.

Main goal:
Make the game visually use the v11 assets instead of rectangle-style drawings, while keeping the game stable and playable.

Asset files:
- assets/world/crane_trolley.png
- assets/world/hook_assembly.png
- assets/world/cargo_crate.png
- assets/world/delivery_dock.png
- assets/world/hazard_barrier.png
- assets/world/steel_beam.png
- assets/world/industrial_fan.png
- assets/world/moving_gate.png
- assets/world/moving_dock.png
- assets/backgrounds/yard_background_1280x720.png
- assets/effects/wind_arrow.png
- assets/effects/wind_zone_overlay.png
- assets/ui/menu_button_normal.png
- assets/ui/menu_button_hover.png
- assets/ui/menu_button_selected.png
- assets/ui/menu_panel.png
- assets/ui/progress_bar_frame.png

CRITICAL BUILD RULE:
These are PNG files. First check whether SDL2_image is available in the project/folder/system.

If SDL2_image is available:
- Add SDL2_image properly.
- Include SDL_image.h.
- Initialize IMG_Init(IMG_INIT_PNG).
- Load PNG textures with IMG_LoadTexture or IMG_Load + SDL_CreateTextureFromSurface.
- Quit SDL_image at shutdown.
- Update build_crane_game.bat with the correct SDL2_image include/lib/runtime usage.
- Make sure SDL2_image.dll is copied or documented.

If SDL2_image is NOT available:
- Do NOT break the build.
- Keep procedural SDL drawings as fallback.
- Report clearly that PNG integration requires SDL2_image.
- Still keep the game compiling and playable.

Asset system:
Add an Assets struct with SDL_Texture* fields for all assets.
Load textures once at startup.
Destroy them at shutdown.
If a texture is missing, draw the existing procedural fallback for that object.
No crash on missing asset.

Rendering requirements:
1. Trolley:
Use assets/world/crane_trolley.png.
Keep it clearly visible.
Preserve aspect ratio.
Anchor it to the crane rail and make the cable/hook point align under it.
Do not make it too large or too tiny.

2. Hook/cable:
Use assets/world/hook_assembly.png if it visually fits.
Continue drawing cable as an SDL line because its length changes dynamically.
Cable must connect trolley/hook to crate.

3. Cargo:
Use assets/world/cargo_crate.png.
Align sprite center with physical cargo position.
Collision rectangle should approximately match visible crate.

4. Delivery bay:
Use assets/world/delivery_dock.png.
Visible dock should match the win zone reasonably.
Keep the stability progress bar readable.

5. Obstacles:
Use hazard_barrier.png and steel_beam.png where appropriate.
Collidable obstacles must be visually distinct from background.
Collision should match visible obstacle bounds reasonably.

6. Wind:
Use industrial_fan.png, wind_arrow.png, and/or wind_zone_overlay.png.
Animate/repeat wind arrows if easy.
Wind direction must be obvious.

7. Moving gate:
Use moving_gate.png.
It must be clearly dangerous/collidable.
Keep motion path faint and readable.

8. Moving dock:
Use moving_dock.png.
It must be visibly the moving target.
Win zone should follow it.

9. Background:
Use yard_background_1280x720.png.
Scale it to fill the game view.
Do not let it interfere with mouse hitboxes or layout.

10. UI:
Use menu button/panel assets only if they do not break usability.
Button hitboxes must match visible buttons exactly.
Mouse hover/click must work in windowed, resized, and fullscreen modes.

Fullscreen/mouse:
Preserve the current fullscreen/full-view system.
Mouse coordinate conversion must remain correct after using assets.
Do not reintroduce letterboxing click bugs.

Testing checklist:
- build_crane_game.bat succeeds
- crane_game.exe launches
- no missing texture crash
- main menu mouse clicks work
- level select mouse clicks work
- how-to-play back button works
- F11 fullscreen still works
- all 6 levels load
- Level 1 is playable and winnable
- trolley/cargo/dock visibly use v11 assets if SDL2_image is available
- fallback works if assets cannot be loaded

Final response:
Report:
1. Files modified.
2. Whether SDL2_image was available and used.
3. Exact build/run command.
4. Assets integrated.
5. Fallback behavior.
6. Confirmation that mouse/fullscreen still works.
7. Any remaining issues.